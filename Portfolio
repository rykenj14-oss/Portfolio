<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ryken J. Wernofsky | Leadership E-Portfolio</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --bg: #f4f6f8;
      --bg-card: #ffffff;
      --text: #222222;
      --text-muted: #555555;
      --accent: #0b3d91;
      --accent-soft: #e6edf7;
      --border: #e0e3ea;
    }
    body.dark {
      --bg: #0b1220;
      --bg-card: #111827;
      --text: #e5e7eb;
      --text-muted: #9ca3af;
      --accent: #60a5fa;
      --accent-soft: #1f2937;
      --border: #1f2937;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      transition: background 0.3s ease, color 0.3s ease;
    }

    /* NAVBAR */
    .navbar {
      position: sticky;
      top: 0;
      z-index: 100;
      background: var(--bg-card);
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 1.5rem;
    }
    .nav-left {
      font-weight: 600;
      color: var(--accent);
    }
    .nav-links {
      display: flex;
      gap: 1rem;
      font-size: 0.95rem;
    }
    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      padding: 0.25rem 0.5rem;
      border-radius: 999px;
    }
    .nav-links a:hover {
      background: var(--accent-soft);
      color: var(--accent);
    }
    .nav-right {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .toggle-btn {
      border: 1px solid var(--border);
      background: var(--bg-card);
      color: var(--text);
      border-radius: 999px;
      padding: 0.25rem 0.75rem;
      font-size: 0.8rem;
      cursor: pointer;
    }

    header {
      padding: 2rem 1.5rem 1rem;
      text-align: center;
    }
    header h1 {
      margin: 0;
      font-size: 2.2rem;
    }
    header p {
      margin: 0.3rem 0;
      color: var(--text-muted);
    }
    header .tagline {
      font-style: italic;
      margin-top: 0.5rem;
      color: var(--accent);
    }

    main {
      max-width: 1000px;
      margin: 0 auto 3rem;
      padding: 0 1.5rem 2rem;
    }

    .card {
      background: var(--bg-card);
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 1.75rem;
      border: 1px solid var(--border);
      box-shadow: 0 8px 20px rgba(15, 23, 42, 0.06);
    }

    h2 {
      margin-top: 0;
      color: var(--accent);
      border-bottom: 2px solid var(--border);
      padding-bottom: 0.3rem;
      margin-bottom: 1rem;
      font-size: 1.3rem;
    }
    h3 {
      margin-bottom: 0.3rem;
      color: var(--text);
      font-size: 1.05rem;
    }
    ul {
      padding-left: 1.2rem;
      margin: 0.4rem 0;
    }
    .two-col {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.5rem;
    }
    .pill {
      display: inline-block;
      padding: 0.25rem 0.6rem;
      background: var(--accent-soft);
      color: var(--accent);
      border-radius: 999px;
      margin: 0.2rem;
      font-size: 0.8rem;
    }
    .meta {
      font-size: 0.85rem;
      color: var(--text-muted);
    }
    a {
      color: var(--accent);
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }

    .banner-wrapper {
      max-width: 1000px;
      margin: 0 auto 1.5rem;
      padding: 0 1.5rem;
    }
    .banner-card {
      background: var(--bg-card);
      border-radius: 0.75rem;
      padding: 0.75rem;
      border: 1px solid var(--border);
      box-shadow: 0 8px 20px rgba(15, 23, 42, 0.06);
    }
    .banner-card img {
      width: 100%;
      border-radius: 0.5rem;
      display: block;
      object-fit: cover;
      max-height: 360px;
    }
    .section-image {
      width: 100%;
      max-width: 360px;
      border-radius: 0.75rem;
      border: 1px solid var(--border);
      display: block;
      object-fit: cover;
    }
    .section-image-small {
      width: 100%;
      max-width: 260px;
      border-radius: 0.75rem;
      border: 1px solid var(--border);
      display: block;
      object-fit: cover;
    }
    .img-row {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: flex-start;
    }

    footer {
      text-align: center;
      padding: 1.5rem;
      background: var(--bg-card);
      color: var(--text-muted);
      border-top: 1px solid var(--border);
      font-size: 0.85rem;
    }

    @media (max-width: 600px) {
      .nav-links {
        display: none;
      }
    }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <nav class="navbar">
    <div class="nav-left">Ryken J. Wernofsky</div>
    <div class="nav-links">
      <a href="#about">About</a>
      <a href="#profile">Profile</a>
      <a href="#experience">Experience</a>
      <a href="#community">Community</a>
      <a href="#education">Education</a>
      <a href="#contact">Contact</a>
    </div>
    <div class="nav-right">
      <button class="toggle-btn" id="themeToggle">Dark mode</button>
    </div>
  </nav>

  <!-- HEADER -->
  <header>
    <h1>Leadership in Service</h1>
    <p>Current Operations Officer & Law Enforcement Coordinator · U.S. Coast Guard</p>
    <p class="tagline">Transformational leadership, operational excellence, and service-driven impact.</p>
  </header>

  <!-- SERVICE BANNER -->
  <div class="banner-wrapper">
    <div class="banner-card">
      <img src="sailing-ship.jpg.jpg" alt="Service Banner - Sailing ship at sea" />
    </div>
  </div>

  <main>

    <!-- ABOUT -->
    <section id="about" class="card">
      <h2>About Me</h2>
      <div class="two-col">
        <div>
          <p>
            I am currently serving as the Current Operations Officer and Law Enforcement Coordinator for Sector Puget Sound.
            With 17 years of combined enlisted and commissioned military experience, I bring a strong foundation of leadership,
            operational excellence, and service.
          </p>
        </div>
        <div>
          <img class="section-image" src="memorial.jpg.jpg" alt="Ryken at Coast Guard Academy memorial" />
        </div>
      </div>
    </section>

    <!-- PROFILE -->
    <section id="profile" class="card">
      <h2>Leadership Profile</h2>
      <div class="two-col">
        <div>
          <h3>Core Attributes</h3>
          <p>
            <span class="pill">Maturity</span>
            <span class="pill">Humility</span>
            <span class="pill">Work Ethic</span>
            <span class="pill">Technical Expertise</span>
            <span class="pill">Integrity</span>
            <span class="pill">Service</span>
          </p>
        </div>
        <div>
          <h3>Leadership Approach</h3>
          <ul>
            <li>Transformational Leadership</li>
            <li>Servant Leadership</li>
            <li>Operational Leadership</li>
            <li>Development Focus</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- EXPERIENCE -->
    <section id="experience" class="card">
      <h2>Leadership Experience</h2>

      <div class="img-row" style="margin-bottom:1rem;">
        <div style="flex:1 1 260px;">
          <h3>Assistant Engineering Petty Officer · USCG Station Port Angeles</h3>
          <p class="meta">July 2020 – Present</p>
          <ul>
            <li>Led engineering department maintaining four Coast Guard assets.</li>
            <li>Converted maintenance records from paper to electronic.</li>
            <li>Responded to 20+ distress calls.</li>
          </ul>
        </div>
        <div>
          <img class="section-image-small" src="boat-wake.jpg.jpg" alt="Boat wake at sea" />
        </div>
      </div>
    </section>

    <!-- COMMUNITY -->
    <section id="community" class="card">
      <h2>Community & Service Leadership</h2>
      <div class="two-col">
        <div>
          <ul>
            <li>Head Coach – Sequim Junior Soccer & Little League.</li>
            <li>Campaign Coordinator – Coast Guard Mutual Assistance.</li>
            <li>Partners in Education Volunteer.</li>
          </ul>
        </div>
        <div>
          <div class="img-row">
            <img class="section-image-small" src="classroom.jpg.jpg" alt="Speaking to students in a classroom" />
            <img class="section-image-small" src="family.jpg.jpg" alt="Family on boardwalk by the water" />
          </div>
        </div>
      </div>
    </section>

    <!-- EDUCATION -->
    <section id="education" class="card">
      <h2>Education</h2>
      <div class="two-col">
        <div>
          <ul>
            <li>AA in Interdisciplinary Studies (4.0 GPA)</li>
            <li>BA in Management (in progress)</li>
          </ul>
        </div>
        <div>
          <img class="section-image" src="diploma.jpg.png" alt="American Military University diploma" />
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section id="contact" class="card">
      <h2>Contact</h2>
      <p><strong>Email:</strong> ryken.j.wernofsky@uscg.mil</p>
      <p><strong>Phone:</strong> 1-360-207-9395</p>
      <p><strong>Location:</strong> Port Angeles, Washington</p>
    </section>

  </main>

  <footer>
    © 2026 · Leadership E-Portfolio · Ryken J. Wernofsky
  </footer>

  <script>
    const toggleBtn = document.getElementById('themeToggle');
    const storedTheme = localStorage.getItem('theme');

    if (storedTheme === 'dark') {
      document.body.classList.add('dark');
      toggleBtn.textContent = 'Light mode';
    }

    toggleBtn.addEventListener('click', () => {
      document.body.classList.toggle('dark');
      const isDark = document.body.classList.contains('dark');
      toggleBtn.textContent = isDark ? 'Light mode' : 'Dark mode';
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
  </script>

</body>
</html>
